{% extends 'admin.html.twig' %}

{% block body %}
    <div>
        <button class="fa fa-chevron-right fa-2x btn btn-default" aria-hidden="true" id="menu-btn"></button>
    </div>


    {% if productUuids|length > 0 %}
    {% set orderNum = 0 %}
    {% for uuid in productUuids %}
        {% set orderNum = orderNum +1 %}

        <div class="text-right width-100">


            <div class="btn btn-labeled btn-default">
                <span class="btn-label">
                    <i class="fa fa-chevron-circle-left"></i>
                </span>
                <a href="/view-order/{{ uuid.noticeId }}">Order.{{ orderNum }}</a>

            </div>
            <br>
        </div>

    {% endfor %}
    <div class="container">
        <div id="order-panel" class="jumbotron ">
            <div class="col-md-6">
                {% set totalPrice = 0 %}
                {% for notice in notices %}
                    <p>{{notice.productId.name}} x {{notice.quantity}} </p>
                    {% if notice.comment is not null %}
                        <p class="comment text-info">({{notice.comment}})</p>
                    {% endif %}
                    
                    {% set totalPrice = totalPrice + notice.productId.price * notice.quantity %}
                {% endfor %}
     
                
                <div class="container col-xs-4">
                    <div class="row bg-info">
                        <h4 class="">Total Price: {{ totalPrice }}&euro;</h4>
                    </div>
                </div>
            </div>
                <div class="col-md-6 bg-success">
                    <br>
                    {% if notices|length > 0 %}
                        <h4 class="col-md-6">{{ notices[0].userId.username }}</h4>
                        <h4 class="col-md-6">{{ notices[0].userId.lastname }}</h4>
                        <h4 class="col-md-6">{{ notices[0].userId.phone }}</h4>
                        <h4 class="col-md-6">{{ notices[0].userId.address }}</h4>
                        <h4 class="col-md-6">{{ notices[0].userId.email }}</h4>
                    {% endif %}
                </div>
                <div class="col-md-4  pull-right margin-top-50">
                    <p>Order send at:</p>
                    <div class="panel col-md-6">
                        {% if notices|length > 0 %}
                            <span class="bg-info">{{ notices[0].dateOrder|date("d/m/y") }}</span> -
                            <span class="bg-warning">{{ notices[0].dateOrder|date("h:i:s") }}</span>
                        {% endif %}
                    </div> 
                </div>
            {% if notices|length > 0 %}
                <div class="col-xs-12">
                        <h3>Extra:</h3>
                        <h2 class="panel descr-panel">{{ notices[0].extra }}</h2>
                </div>
                <div class="col-xs-6 ">
                        <a href="/cancel-order/{{ notices[0].noticeId }}" class="btn btn-danger">Order Cancel</a>
                </div>
                <div class="col-xs-6">
                        <a href="/finish-order/{{ notices[0].noticeId }}" class="btn btn-primary  pull-right">Order finished</a>
                </div>
            {% endif %}
        </div>
    </div>
    {% else %}
        <h1 class="text-center text-danger">No Orders</h1>
    {% endif %}

{% endblock %}